name = "ala-archa-telegram"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Токен и Chat ID НЕ хранятся здесь — они задаются командой:
#   npx wrangler secret put TELEGRAM_BOT_TOKEN
#   npx wrangler secret put TELEGRAM_CHAT_ID
# и живут зашифрованными на стороне Cloudflare.
